// Accepts n (int) from standard input and writes the first n numbers from the fibonacci sequence to standard output.

// Entry point.
void main() {
    int n = read();
    int a = -1;
    int b = 1;
    int i = 0;
    while (i < n) {
        i = i + 1;
        int temp = a;
        a = b;
        b = b + temp;
        write(b);
    }
}
